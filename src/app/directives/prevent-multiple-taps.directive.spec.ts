import { PreventMultipleTapsDirective } from './prevent-multiple-taps.directive';

describe('PreventMultipleTapsDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventMultipleTapsDirective();
    expect(directive).toBeTruthy();
  });
});
